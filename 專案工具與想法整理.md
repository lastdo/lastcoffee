# 巴哈耳機普查工具：工具與想法整理

## 1. 專案目標

- 貼給別人就是正式可用網址，不再使用 `localhost`
- 不做會員註冊、不做複雜流程
- 使用者填完表單後，可以直接產生巴哈可貼文格式
- 管理端可以看品牌、型號、分類排行
- 品牌可以擴充，並保留審核機制避免亂名
- 技術路線優先採用已熟悉的 Streamlit Community Cloud 部署方式

---

## 2. 目前技術路線

目前專案已改成 Streamlit 版本，入口檔案是：

```text
streamlit_app.py
```

部署依賴檔案是：

```text
requirements.txt
```

本機測試指令：

```powershell
python -m streamlit run streamlit_app.py
```

部署平台：

```text
Streamlit Community Cloud
```

部署後網址會像：

```text
https://lastcoffee.streamlit.app
```

或改成更中性的名稱：

```text
https://audio-census.streamlit.app
https://coffee-census.streamlit.app
```

---

## 3. 使用者流程

1. 開啟 Streamlit 公開網址
2. 輸入巴哈 ID
3. 新增多筆設備：
   - 類型
   - 品牌
   - 型號
   - 備註
4. 送出資料
5. 取得巴哈可貼文格式
6. 複製後貼回巴哈

目前編輯方式：

1. 到「查詢/編輯」
2. 輸入巴哈 ID
3. 載入自己的舊資料
4. 修改後重新送出

後續若要防止他人亂改，可再加入編輯碼。

---

## 4. 功能模組清單

### A. 表單填寫器

- 固定設備類型選單
- 品牌選單
- 可輸入新品牌，新品牌預設為 `pending`
- 型號獨立欄位
- 單筆設備備註
- 整體補充備註
- 多筆設備新增/移除

### B. 巴哈貼文產生器

- 依設備分類自動分段
- 產生一致格式，減少人工排版
- 方便直接複製貼到巴哈

### C. 查詢與編輯

- 用巴哈 ID 查詢既有資料
- 可載入舊資料編輯
- 可刪除自己的紀錄

### D. 統計頁

- 填寫紀錄數
- 設備總數
- 正式品牌數
- 待確認品牌數
- 類型排名
- 品牌排名
- 型號排名
- CSV/JSON 匯出

### E. 品牌管理

- 正式品牌：`approved`
- 待審品牌：`pending`
- 拒絕品牌：`rejected`
- alias 合併，例如 `FiiO / Fiio / FIIO / 飛傲`
- 通過品牌後，同步更新既有設備顯示名稱

---

## 5. 資料儲存策略

目前版本仍使用相容的 JSON 格式：

```text
data/census.json
```

資料結構維持：

```text
brands
entries
```

這樣做的好處：

- 可以沿用既有資料
- 本機測試很快
- 邏輯單純，適合先確認流程

但正式公開收資料前，建議不要只依賴 Streamlit Cloud 的本機檔案。Streamlit Community Cloud 的 local file storage 不適合當長期資料庫，重新部署或平台回收資源時可能有資料遺失風險。

正式資料儲存建議改成你已熟悉的雲端 JSON 流程，或擇一：

- Google 雲端 JSON
- Google Sheets
- Firestore
- Supabase

---

## 6. 建議架構

短期架構：

```text
Streamlit UI
  -> data/census.json
```

正式收資料架構：

```text
Streamlit UI
  -> 雲端 JSON / Google Sheets / Firestore
```

也就是說，Streamlit 負責畫面與互動，雲端資料層負責多人共享與長期保存。

---

## 7. 專案檔案定位

```text
streamlit_app.py
```

主要 Streamlit app。包含填寫、查詢/編輯、統計、後台品牌管理。

```text
requirements.txt
```

Streamlit Cloud 部署依賴。

```text
STREAMLIT_DEPLOY.md
```

Streamlit Cloud 部署步驟。

```text
data/census.json
```

目前測試用資料檔。正式收資料前建議改接雲端儲存。

```text
server.py / index.html / app.js / styles.css
```

舊版本機 HTML/JS 方案，可暫時保留作為參考；主線改以 `streamlit_app.py` 為準。

---

## 8. 風險與對策

風險：Streamlit Cloud 本機 JSON 不適合長期保存  
對策：正式收資料前接雲端 JSON 或其他外部儲存

風險：有人用別人的巴哈 ID 修改資料  
對策：加入編輯碼，或至少在正式版限制刪除/覆蓋行為

風險：品牌命名爆炸  
對策：新品牌預設 pending，透過後台審核與 alias 合併

風險：社群不想離開巴哈  
對策：維持「填表後產生巴哈貼文」策略，讓網站只是輔助填寫與統計

---

## 9. 建議開發順序

1. 先用 Streamlit Cloud 部署可公開測試版本
2. 確認表單、貼文、統計、品牌審核流程順手
3. 接上你熟悉的雲端 JSON 儲存
4. 補編輯碼，避免他人誤改資料
5. 正式公開網址給社群填寫
6. 普查結束後匯出 CSV/JSON 做排行整理

---

## 10. 成功標準

- 使用者點開網址即可填寫
- 不需要安裝任何東西
- 表單完成率高，不需要額外教學
- 貼文格式可直接貼到巴哈
- 管理端可以即時看到品牌與型號排行
- 普查結束時可直接匯出資料

