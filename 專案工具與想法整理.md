# 巴哈耳機普查工具：工具與想法整理

## 1. 專案目標（不變原則）

- 不做會員註冊、不做複雜流程
- 使用者只要填寫表單就能送出
- 送出後可一鍵產生巴哈可貼文格式
- 管理端能即時統計品牌/型號/分類排行
- 品牌可擴充，且有審核機制避免亂名

---

## 2. 使用者流程（建議）

1. 開啟頁面
2. 輸入巴哈 ID
3. 新增多筆設備（類型、品牌、型號、備註）
4. 送出
5. 取得：
   - 可貼巴哈的格式化文字
   - 編輯碼（無會員修改用）

後續修改流程：

1. 輸入巴哈 ID + 編輯碼
2. 載入舊資料
3. 修改後送出覆蓋

---

## 3. 功能模組清單

### A. 表單填寫器

- 固定設備類型下拉選單
- 品牌搜尋（下拉 + datalist）
- 型號獨立欄位
- 單筆備註、整體備註
- 多筆設備新增/刪除

### B. 巴哈貼文產生器

- 依分類自動分段輸出
- 一鍵複製
- 格式一致，減少人工排版

### C. 統計後台

- 品牌總持有數
- 型號排行
- 耳罩式前十品牌
- 耳道式前十品牌
- 桌面型前端前十品牌
- 便攜型前端前十品牌
- CSV/JSON 匯出

### D. 品牌主檔與審核

- 正式品牌（approved）
- 待審品牌（pending）
- 拒絕品牌（rejected）
- alias 合併（FiiO/Fiio/FIIO/飛傲）

### E. 無會員可編輯機制

- 每筆提交發一組編輯碼
- 修改需要巴哈 ID + 編輯碼
- 不需要註冊帳號

---

## 4. 技術路線（由簡到穩）

## 階段 1：純前端 MVP（已完成方向）

- 技術：`HTML + CSS + JavaScript + localStorage`
- 優點：快、零門檻、立即可測
- 缺點：資料不共享，統計只看本機

## 階段 2：輕後端集中資料（建議下一步）

- 前端維持現在頁面
- 新增 API + 資料庫
- 所有人共用同一份資料，統計才有意義

可選方案：

1. Supabase（PostgreSQL + REST，開發快）
2. Firebase（文件型，前端整合快）
3. 自架後端（Node.js + PostgreSQL，控制力高）

---

## 5. 你可能會用到的工具

## 前端/UI

- 原生 `HTML/CSS/JS`（目前架構）
- 可選：`Vite`（若要模組化）

## 後端/API（擇一）

- `Supabase`
- `Firebase`
- `Node.js + Express + PostgreSQL`

## 部署

- 靜態前端：`Cloudflare Pages` / `Netlify` / `GitHub Pages`
- 全端：`Render` / `Railway` / `Fly.io` / VPS

## 管理與觀測

- `Plausible` 或 `GA4`：看填寫轉換
- `Sentry`：前端錯誤追蹤

---

## 6. 資料庫設計草案（精簡）

### `entries`（填寫主表）

- `id`
- `bahamut_id`
- `edit_code_hash`
- `general_note`
- `created_at`
- `updated_at`

### `entry_devices`（設備明細）

- `id`
- `entry_id`
- `device_type`
- `brand_id`
- `model`
- `note`

### `brands`（品牌主檔）

- `id`
- `english_name`
- `chinese_name`
- `status` (`approved/pending/rejected`)
- `aliases` (JSON 或關聯表)

---

## 7. 命名與資料規則

- 品牌、型號分欄，不混寫
- 類型固定選單，不自由輸入
- 新品牌預設 `pending`
- 統計只使用 `approved` 品牌
- 同一巴哈 ID 的多版本資料：
  - 可選擇統計「只算最新」
  - 或保留歷史版本供回溯

---

## 8. 風險與對策

- 風險：有人亂改別人資料  
  對策：編輯碼驗證（無會員也能防呆）

- 風險：品牌命名爆炸  
  對策：pending 審核 + alias 合併

- 風險：社群不想離開巴哈  
  對策：維持「填表後一鍵貼巴哈」策略

---

## 9. 建議開發順序

1. 完成「編輯碼」流程
2. 接資料庫與 API
3. 改為多人共用統計
4. 後台補品牌合併工具
5. 正式部署公開網址

---

## 10. 成功標準（可量化）

- 表單完成率 > 80%
- 手動整理時間下降 > 90%
- 品牌重複命名比例持續下降
- 普查結束當天可直接產出排行
